{
  "dependencies": {
    "markdownlint-cli": "^0.30.0"
  },
  "scripts": {
    "lint": "npx markdownlint '**/*.md' --ignore **/node_modules/** -c .markdownlint.json"
  },
  "devDependencies": {
    "markdownlint": "^0.26.1"
  }
}
